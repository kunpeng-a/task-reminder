# 更新日志

本项目的所有显著变更都记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

## [1.0.0] - 2026-06-09

任务提醒（自用版）首个可用版本：Electron + React + TypeScript + Vite。

### 功能
- **三类提醒**：周期（每 N 分钟）、定时（指定日期时间一次性）、每日（每天多个 HH:mm）。
- **全屏遮罩提醒**：触发时弹出置顶半透明全屏窗口，显示任务名/内容/当前时间 + 倒计时进度条；支持"一直显示"模式（手动关闭）。
- **调度核心**：`computeNextTrigger` 纯函数 + 主进程心跳（25s）+ 当天内 catch-up；带宽限补弹，不遗漏提醒。
- **周期计时锚点**：以"启用时刻 `enabledAt`"为锚——启用即从满间隔计时，关闭清零，重新开启重新计时；遮罩关闭后才重新开始下一轮。
- **遮罩配置两级覆盖**：每个任务的 `mask`（自动关闭/倒计时秒数/背景图）与 `soundFile` 为空时回退全局默认。
- **音效**：内置叮咚/水滴/木鱼/静音；全局默认与**每个任务**均可单独设置，且每任务支持选择自定义音频文件。
- **自定义背景壁纸**：每任务可选本地图片作为遮罩背景。
- **托盘常驻**：最小化到系统托盘、右键暂停/恢复所有提醒、开机自启。
- **可靠性**：单实例锁、全屏独占逃生舱（演示/游戏时不强弹）、`powerMonitor` 唤醒后补弹、snooze 稍后提醒。
- **持久化**：electron-store 本地 JSON，重启自动恢复调度。

### 工程
- 调度核心 `computeNextTrigger` 单元测试（14 个用例）。
- 高保真还原 `Prototype/` 设计稿（Neutral Modern 设计系统 token）。
